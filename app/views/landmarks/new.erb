<h1>Create a New Landmark:</h1>
<form action="/landmarks" method="post">
  <h2><label>Landmark name: <input type="text" name="landmark[name]" id="landmark_name"></label></h2>
  <h2><label>Year completed: <input type="text" name="landmark[year_completed]" id="landmark_year_completed"></label></h2>
  <h2>Select Figure:</h2>
  <% Figure.all.each do |figure| %>
  <label><input type="radio" name="landmark[figure_id]" value="<%= figure.id %>" id="figure_<%= figure.id %>"><%= figure.name %></label>
  <% end %>
  <h4>or create a new figure: <label><input type="text" name="figure[name]" id="new_title"></label></h4>
  <div><input type="submit" value="Create New Landmark"></div>
</form>
