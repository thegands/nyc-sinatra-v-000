<h1>Edit a Landmark:</h1>
<form action="/landmarks/<%= @landmark.id %>" method="post">
  <h2><label>Landmark name: <input type="text" name="landmark[name]" id="landmark_name" value="<%= @landmark.name %>"></label></h2>
  <h2><label>Year completed: <input type="text" name="landmark[year_completed]" id="year_completed" value="<%= @landmark.year_completed %>"></label></h2>
  <h2>Select Figure:</h2>
  <% Figure.all.each do |figure| %>
  <label><input type="radio" name="landmark[figure_id]" value="<%= figure.id %>" id="figure_<%= figure.id %>"><%= figure.name %></label>
  <% end %>
  <h4>or create a new figure: <label><input type="text" name="figure[name]" id="new_title"></label></h4>
  <div><input type="submit" value="Edit Landmark"></div>
</form>
